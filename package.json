{
  "name": "undead-sector",
  "version": "1.0.0",
  "description": "Undead Sector: Last Stand game frontend",
  "main": "bundle.js",
  "scripts": {
    "start": "http-server . --port ${PORT:-8080}",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  }
}